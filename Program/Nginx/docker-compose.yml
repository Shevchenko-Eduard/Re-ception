services:

  server-nginx:
    image: nginx:1.30.0
    container_name: server-nginx
    ports:
      - "${NGINX_HTTP_PORT}:80"
      - "${NGINX_HTTPS_PORT}:443"
    volumes:
      - ./conf/:/etc/nginx/
    